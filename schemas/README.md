# Schemas

This directory will contain the canonical extraction schema used to normalize heterogeneous bank statements.

The schema will be defined only after the supplied sample documents are systematically assessed.
